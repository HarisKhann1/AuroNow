[phases.setup]
nixPkgs = ["python310", "gcc", "pkg-config", "libmysqlclient", "mysql80"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py runserver 0.0.0.0:$PORT"